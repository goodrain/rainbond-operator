apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rainbond.fullname" . }}-test-connection"
  labels:
    {{- include "rainbond.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rainbond.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

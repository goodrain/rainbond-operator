apiVersion: v1
kind: Service
metadata:
  name: {{ include "rainbond.fullname" . }}
  labels:
    {{- include "rainbond.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rainbond.selectorLabels" . | nindent 4 }}
